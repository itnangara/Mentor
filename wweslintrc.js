module.exports = {
    // Other ESLint configurations...
    overrides: [
        {
            files: ['**/*.csp'], // Match .csp files
            rules: {
                // Add specific rules or overrides for .csp files
            },
        },
    ],
};
